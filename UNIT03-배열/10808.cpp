#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int a, b, c, mul;
    string mul_str;
    int num[10];

    cin >> a;
    cin >> b;
    cin >> c;
    mul = a * b * c;
    mul_str = to_string(mul);
//    cout << mul_str << " " << mul_str[2];
    for (int i=0; i<=9; i++) {
    
    }
}